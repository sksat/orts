// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { CommandConfig } from "./CommandConfig";
import type { GravityFieldConfig } from "./GravityFieldConfig";
import type { GroundStationConfig } from "./GroundStationConfig";
import type { IntegratorConfig } from "./IntegratorConfig";
import type { SatelliteConfig } from "./SatelliteConfig";

/**
 * JSON/TOML/YAML simulation configuration.
 *
 * Also the payload of the `start_simulation` WebSocket message, so the
 * whole tree derives [`TS`]. Fields the server defaults when absent are
 * `#[ts(optional)]` so TypeScript clients may omit them too.
 */
export type SimConfig = { body?: string, dt?: number, output_interval?: number, stream_interval?: number, epoch?: string, integrator?: IntegratorConfig, atmosphere?: string, f107?: number, ap?: number, space_weather?: string, 
/**
 * 球面調和重力場 (ICGEM `.gfc`)。指定すると J2/J3/J4 の `ZonalGravity` の
 * 代わりに `SphericalHarmonicGravity` を登録し、中心項の μ もファイルの GM
 * に置き換える。table 不在が「従来どおり zonal」なので `Option` (空 table と
 * 区別する)。Earth 専用。
 */
gravity_field?: GravityFieldConfig, 
/**
 * 伝播する慣性系。`"simple-eci"` (既定, ERA のみ) か `"gcrs"`
 * (IAU 2006/2000A CIO chain + 観測 EOP)。`gcrs` は軌道のみの `orts run`
 * で使える。姿勢・コントローラ・`serve` は SimpleEci 固定。
 */
frame?: string, 
/**
 * `frame = "gcrs"` の EOP: `"auto"` (IERS から取得, 24h キャッシュ)、
 * ファイルパス (IERS finals2000A)、`"zero"` (観測 EOP なし)。
 */
eop?: string, duration?: number, satellites?: Array<SatelliteConfig>, 
/**
 * 時刻指定コマンドシーケンス（FSW への C&T アップリンク）。
 * 各エントリは指定 sim 時刻に対象衛星のコントローラへ配送される。
 * TOML では array-of-tables の慣習に従い `[[command]]`（単数）で宣言する。
 */
command?: Array<CommandConfig>, 
/**
 * 地上局定義（contact window 検出用）。Earth 中心のシミュレーション
 * でのみ有効。TOML では `[[ground_station]]`（単数）で宣言する。
 */
ground_station?: Array<GroundStationConfig>, };
