[package]
name = "orts-plugin-sdk"
version = "0.0.0"
edition = "2024"
publish = false
description = "SDK for writing orts WASM plugin guests (callback and main-loop styles)"

# Independent workspace — plugin crates are not part of the parent orts workspace.
[workspace]

# No runtime dependencies. The macro generates code that references
# the consumer crate's `bindings` module (generated by cargo-component).
